export enum Feature {
    Auth       = "auth",
    Data       = "data",
    Chart      = "chart",
    Summary    = "summary",
    Adjustment = "adjustment",
}

export enum Vault {
    User = "user",
}
